{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/mock/(.*)", "dest": "/api/mock/[...slug].js" },
    { "src": "/api/(.*)", "dest": "/api/$1.js" }
  ],
  "cleanUrls": true
}
